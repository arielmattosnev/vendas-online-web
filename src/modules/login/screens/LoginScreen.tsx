import { BackgroundImg } from '../styles/LoginScreen.styles';

const LoginScreen = () => {
  return (
    <div>
      <BackgroundImg src='./background.png' />
    </div>
  );
};

export default LoginScreen;
